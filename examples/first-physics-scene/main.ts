/**
 * §93 first-physics-scene — thin entry over `examples/physics-playground`.
 *
 * Gravity, collisions, impulses and sensors already run there, 2D and 3D
 * through one API. This directory exists so the §93 name is a real,
 * buildable site rather than a `.gitkeep` (AUDIT-120 S-8, closed 2026-09-06).
 */
import "../physics-playground/main.ts";
