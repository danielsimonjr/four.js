/**
 * §93 mixed-scene — thin entry over `examples/physics-playground`.
 *
 * That page already steps a 2D world and a 3D world side by side through
 * one API. This directory exists so the §93 name is a real, buildable site
 * rather than a `.gitkeep` (AUDIT-120 S-8, closed 2026-09-06).
 */
import "../physics-playground/main.ts";
