/**
 * §93 first-animated-scene — thin entry over `examples/first-2d-scene`.
 *
 * Tweens, a clip and a timeline already run in that scene. This directory
 * exists so the §93 name is a real, buildable site rather than a `.gitkeep`
 * (AUDIT-120 S-8, closed 2026-09-06). No packages were edited.
 */
import "../first-2d-scene/main.ts";
