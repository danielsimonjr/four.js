# @four/physics-box2d

Box2D solver adapter. Part of [four.js](../../README.md) — **scaffold only; no implementation yet.**

2D solver adapter backed by Box2D, implementing the shared `PhysicsSolverAdapter` interface and declaring its capabilities.

Specification: §37, §102 in [`docs/SPECIFICATION.md`](../../docs/SPECIFICATION.md).

## Layout

- `src/` — implementation (strict TypeScript, ESM)
- `tests/` — unit tests (Vitest), colocated per package (§92)
