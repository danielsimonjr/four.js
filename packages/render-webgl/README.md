# @four/render-webgl

WebGL 2 backend. Part of [four.js](../../README.md) — **scaffold only; no implementation yet.**

WebGL 2 rendering backend — the MVP backend (§120), first implemented in Phase 3 (§106).

Specification: §62, §106, §120 in [`docs/SPECIFICATION.md`](../../docs/SPECIFICATION.md).

## Layout

- `src/` — implementation (strict TypeScript, ESM)
- `tests/` — unit tests (Vitest), colocated per package (§92)
