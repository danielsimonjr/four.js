# @four/render-webgpu

WebGPU backend. Part of [four.js](../../README.md) — **scaffold only; no implementation yet.**

WebGPU rendering backend (WGSL). Preferred backend under `renderer: "auto"`.

Specification: §62 in [`docs/SPECIFICATION.md`](../../docs/SPECIFICATION.md).

## Layout

- `src/` — implementation (strict TypeScript, ESM)
- `tests/` — unit tests (Vitest), colocated per package (§92)
