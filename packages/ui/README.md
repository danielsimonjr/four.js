# @four/ui

Retained-mode UI. Part of [four.js](../../README.md) — **scaffold only; no implementation yet.**

UI controls as scene nodes, layout engine (absolute/stack/flex/grid/anchor/constraints), and the hidden DOM accessibility mirror.

Specification: §73–75 in [`docs/SPECIFICATION.md`](../../docs/SPECIFICATION.md).

## Layout

- `src/` — implementation (strict TypeScript, ESM)
- `tests/` — unit tests (Vitest), colocated per package (§92)
