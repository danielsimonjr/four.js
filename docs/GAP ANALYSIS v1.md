# four.js — Gap Analysis v1

**Date:** 2026-08-08
**Tree ref:** `e0ddd3b` on branch `claude/tools-integration-rji2sr` (working tree clean; this
analysis is read-only apart from the superseded-pointer paragraph it adds to v0).
**Supersedes:** `docs/GAP ANALYSIS v0.md` (2026-08-05, tree `cff56e7`), which stays in place
as the historical record — its findings, closure plans, and dated closure banners are still
the evidence trail behind every row below.
**Spec baseline:** `docs/SPECIFICATION.md` revision 1.7 (2026-08-06 — §97a landed as part of
the campaign this document reports on).

**Method.** v0 filed 91 findings across three tiers (`A-*` application, `R-*` rendering,
`PH-*` simulation) plus six documentation defects (`D-*`). Between 2026-08-06 and
2026-08-07 a closure campaign landed against them: 26 batches recorded in `CHANGELOG.md`
(24 dated 2026-08-07, 2 dated 2026-08-06) plus two that reached no tracking file at all.
This document re-reads every
one of the 97 filings against the tree at `e0ddd3b` and re-states its status. Where a status
was checked in source rather than taken from a tracking file, the pointer in the table is the
file that carries the evidence; no claim below rests on `CHANGELOG.md`, `TODO.md`, or
`MEMORY.md` alone, because two of the campaign's own batches never reached those files
(see _Tracking integrity_ below).

The v0 ID space is kept verbatim. `S-*` still means `docs/AUDIT-120.md`'s staged items, never
a v0 finding.

---

## 1. Executive summary

### Headline

**The application tier is largely closed, the simulation tier is almost entirely closed, and
the rendering tier is now the project.** Of 97 filings, 42 are closed, 14 are partially
closed, 4 are blocked behind three drafted RFCs, and 37 remain open — and 26 of those 37 open
items are `R-*`. The campaign closed every render finding that was a _keystone_ (`R-4`, `R-5`,
`R-6`, `R-12` base, `R-19`, `R-20`, `R-2`, `R-35`) and almost none that was a _feature_ (no
PBR, no shadows, no multi-light, no colour management, no batching, no 2D vector stack, no
`Text` node, no camera rigs). That is the right order — the blockers fell first — but it means
the remaining backlog is longer in wall-clock terms than the closed half, not shorter.

Two honest qualifications on the closed count:

- Most render closures shipped at a **named tier**, not in full: `R-4` is "minimal" (no
  `readPixels`, no MRT, no float, no samplable depth), `R-5` is "linear-pass" (no transient
  resources, no barriers), `R-6` is "full-screen effect" (2 of 10 §70 effects), `R-12` is the
  abstract base only (0 of 8 missing §57 family members). Each tier is declared in source
  with a dated note naming what it defers, so the tiering is honest — but a reader counting
  "closed" as "§N is done" would be wrong every time.
- The `A-*` closures are the deepest: §84 statistics, §40 units, §96 security, §90
  compatibility, §94 machinery, §45's string renderer form, keyboard input, and the §118
  flagship all exist where nothing existed on 2026-08-05.

### Counts by tier

| Tier                      | Filed | Closed | Partially closed | RFC drafted | Open |
| ------------------------- | ----- | ------ | ---------------- | ----------- | ---- |
| Application (`A-1…A-28`)  | 28    | 13     | 9                | 1           | 5    |
| Rendering (`R-1…R-41`)    | 41    | 9      | 4                | 2           | 26   |
| Simulation (`PH-1…PH-22`) | 22    | 14     | 1                | 1           | 6    |
| Doc defects (`D-1…D-6`)   | 6     | 6      | —                | —           | —    |
| **Total**                 | 97    | 42     | 14               | 4           | 37   |

Four filings are duplicates of two work items and were closed once each: `A-2`/`PH-13` (§40
`UnitSystem`) and `A-22`/`PH-18` (Part X examples). `A-8`/`R-2`/`PH-19` were three filings of
one design (registries) and closed in one packet. `R-22`/`PH-10` are two filings of one RFC.
So the 42 closures represent 38 distinct pieces of work.

### What moved the needle

- **The three v0 keystones all landed.** `R-12`(base) + `R-11` + `R-10`(key 2) on 2026-08-06;
  `R-19` + `R-20` and `R-4` on 2026-08-07. Their dependents lost their blockers: `R-9`,
  `R-13`, `R-22`, `R-28`, `R-30`, `R-32`, `R-35`, `R-5`, `R-6`, `R-7`, `R-18` are all now
  gated on their own work rather than on somebody else's. _`R-8` itself closed 2026-08-09,
  taking `R-10`'s key 4 with it and shrinking `R-27` to `depthMode` + `material[]`._
- **The v0 "silent sections" mostly stopped being silent.** §63 (`R-5`), §70 (`R-6`), §84
  (`A-1`), §96 (`A-23`), §90 (`A-26`), §85's build-mode half (`A-4`) all now have code and a
  record. Still silent, with no note anywhere: **§82** (`A-20`), **§54**'s non-skinning rows
  (`R-22`). _§58 left this list 2026-08-09 (`R-16` closed at the solid-paint + full-stroke
  tier); **§8** (`PH-12`) and **§26/§27 for rigid bodies** (`PH-8`) left it the same day —
  §8 at the physics tier with `RigidBody.space` and an enforced `addBody` refusal, §26/§27
  closed with `ForceFieldSystem` at §39's step 5._
- **The largest single v0 gap shrank but did not close.** `A-21` was "four §93 scenes and both
  flagships are empty directories". `examples/first-3d-scene/main.ts` and
  `examples/flagship/one-scene-everything-moves/main.ts` are written and browser-gated; eight
  examples now have a `main.ts`. §119's motor digital twin shipped 2026-08-08, and three §93
  stand-in directories await an owner retire-or-write decision.
- **Nothing regressed into dishonesty.** Every tier label, every staged `NaN`, every closed
  union that refuses an unshipped member was re-checked against source and holds.

---

## 2. Closed

One line per closed filing. The pointer column names a file that can be opened to check the
claim; where a closure was a decision rather than code, it names the document that records it.
"Tier" in the notes column means the closure is complete against the finding as filed but
narrower than the whole spec section — the residue is listed in §4 or in the source note the
pointer names.

### Application

| ID     | §                | What closed it                                                                                                                                                                                                               | Pointer                                                                             |
| ------ | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| `A-1`  | 84               | `app.stats` with §84's eleven counters (8 measured, 3 staged `NaN`-with-a-reason); presence-is-the-capability                                                                                                                | `packages/diagnostics/src/stats.ts`, `packages/render/src/statistics.ts`            |
| `A-2`  | 40               | `UnitSystem`/`SI_UNITS`/`resolveUnitSystem` + display conversions; display-only enforced by a test                                                                                                                           | `packages/core/src/units.ts`, `tests/integration/units-display.test.ts`             |
| `A-7`  | 45               | `Application.resize` + `width`/`height`/`resolution`/`depthRange` options                                                                                                                                                    | `packages/four/src/application.ts`                                                  |
| `A-8`  | 45, 62           | `renderer: "auto" \| <name>` over an opt-in registry; `four` still imports no backend                                                                                                                                        | `packages/render/src/renderer-registry.ts`, `packages/render-webgl/src/register.ts` |
| `A-9`  | 72, 83           | Per-pointer teardown on `pointerup`/`pointercancel` + the 10 000-gesture test; `pointerType` end to end (2026-08-09), so a mouse keeps its hover across its own release and a UI widget no longer un-highlights when clicked | `packages/input/src/pointer-input.ts`, `packages/input/tests/pointer-type.test.ts`  |
| `A-10` | 72               | `KeyboardInput` + `SceneKeyEvent` + shared three-phase dispatch (keyboard tier)                                                                                                                                              | `packages/input/src/keyboard-input.ts`, `packages/input/src/propagation.ts`         |
| `A-14` | 73, 79           | `registerUISerializers()`; widgets round-trip with box model, layout, §75 record                                                                                                                                             | `packages/four/src/scene-serializers.ts`                                            |
| `A-15` | 79, 6a           | `Node.components` + throw-by-default on an unserializable component                                                                                                                                                          | `packages/scene/src/node.ts`, `packages/serialization/src/serializer.ts`            |
| `A-17` | 79               | `NodeOptions.id` reserves against the counter; duplicate-id refusal in `instantiateScene`                                                                                                                                    | `packages/scene/src/node.ts`                                                        |
| `A-22` | 97, 114–117, 97a | Owner decision: amend the spec. §97a + §97/§114–§117 rewritten and type-checked against the built `.d.ts`                                                                                                                    | `docs/SPECIFICATION.md` (§97a; amendments row 1.7)                                  |
| `A-23` | 96               | Asset `maximumBytes`/`timeoutSeconds`, `parseUntrustedJson`, `UNTRUSTED_INPUT_REJECTED`, CSP grep test                                                                                                                       | `packages/core/src/untrusted.ts`, `tests/integration/security-csp.test.ts`          |
| `A-26` | 90               | `docs/COMPATIBILITY.md`'s five tables; solver block generated from live capability declarations                                                                                                                              | `docs/COMPATIBILITY.md`, `tools/generate-compatibility.mjs`                         |
| `A-28` | —                | Seven false claims corrected in place + a mechanical gate so they cannot recur                                                                                                                                               | `tools/check-docs.mjs`                                                              |

### Rendering

| ID          | §          | What closed it                                                                                                  | Pointer                                                                                 |
| ----------- | ---------- | --------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- |
| `R-2`       | 62, 45     | Backend registry, §62 preference order, fallback with disposal, fail-fast on a named backend                    | `packages/render/src/renderer-registry.ts`                                              |
| `R-4`       | 61, 48, 63 | `RenderTarget` + FBO cache + `render(root, views, interpolation, target)` (minimal tier)                        | `packages/render/src/render-target.ts`, `packages/render-webgl/src/gl-render-target.ts` |
| `R-5`       | 63         | `RenderGraph`: ordered named passes, declared inputs, discovered sampling, `describe()` (linear-pass tier)      | `packages/render/src/render-graph.ts`                                                   |
| `R-6`       | 70         | `EffectRenderPass` + `Renderer.renderEffect`; copy (bit-exact) and colour grade (effect tier)                   | `packages/render/src/effect-pass.ts`, `packages/render-webgl/src/gl-effect.ts`          |
| `R-11`      | 57, 66     | `transparent`/`blendMode` honoured; the dead-alpha defect is fixed                                              | `packages/render-webgl/src/webgl-renderer.ts`                                           |
| `R-19`      | 53         | `BufferGeometry.uvs`/`.colors`, `UnlitMaterial.map`/`.vertexColors`, `LitMaterial.map` — meshes can be textured | `packages/geometry/src/buffer-geometry.ts`, `packages/materials/src/texture.ts`         |
| `R-20`      | 53         | Nine 3D primitives (sphere, cylinder, cone, capsule, torus, lathe, extrude, tube, height field)                 | `packages/geometry/src/primitives-3d.ts`                                                |
| `R-35`      | 113, 120   | `debugDrawStreams`/`applyDebugDrawStreams`; the whole overlay is one draw call                                  | `packages/diagnostics/src/debug-draw.ts`                                                |
| `R-40`      | 118        | The §118 flagship exists and is browser-gated (six measuring tests)                                             | `examples/flagship/one-scene-everything-moves/main.ts`                                  |
| `D-1`…`D-6` | —          | All six documentation defects corrected in place 2026-08-05, four of them now gated                             | `tools/check-docs.mjs`                                                                  |

### Simulation

| ID      | §       | What closed it                                                                                             | Pointer                                                                                |
| ------- | ------- | ---------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| `PH-1`  | 37, 23  | Stage 1 truth table (2026-08-06) + stage 2 `SolverBodyTuningAccess` and a step-top drain; `teleport()`     | `packages/physics/src/body-access.ts`, `packages/physics/src/world.ts`                 |
| `PH-2`  | 37, 20  | `PhysicsWorld.getBodyHandle` / `getColliderHandle` escape hatches                                          | `packages/physics/src/world.ts`                                                        |
| `PH-3`  | 23, 25  | `colliderIds` maintained on destroy; an authored first-collider mass moves to the surviving heir           | `packages/physics-rapier/src/rapier2d-adapter.ts` (mirrored in the 3D adapter)         |
| `PH-4`  | 23      | `derivedMass` mirror; `toDescriptor()` emits `mass` only when authored                                     | `packages/physics/src/rigid-body.ts`                                                   |
| `PH-5`  | 24, 37  | `PhysicsWorld.addCollider` / `removeCollider` with mass re-established in both directions                  | `packages/physics/src/world.ts`, `tests/integration/physics-runtime-colliders.test.ts` |
| `PH-6`  | 34      | `ReplayRecording.worldConfiguration`; lowest-version-that-expresses rule keeps v1 documents byte-identical | `packages/diagnostics/src/recorder.ts`, `packages/diagnostics/src/replay-format.ts`    |
| `PH-7`  | 34, 37  | 3D `#rebuildRegistries` gains the 2D null/validity check and re-asserts `type`/`bodyIdA`/`bodyIdB`         | `packages/physics-rapier/src/rapier3d-adapter.ts`                                      |
| `PH-13` | 40      | One closure with `A-2`                                                                                     | `packages/core/src/units.ts`                                                           |
| `PH-14` | 25      | `PhysicsTuningCapabilities` + a one-shot warning when §25's rolling/spinning friction cannot be honoured   | `packages/physics/src/adapter.ts`, `packages/physics/src/world.ts`                     |
| `PH-15` | 32      | Same mechanism for §32's sleep thresholds                                                                  | `packages/physics/src/world.ts`                                                        |
| `PH-16` | 22, 42  | `RigidBody.type` setter warns once on a registered body, naming `setBodyControlMode`                       | `packages/physics/src/rigid-body.ts`                                                   |
| `PH-17` | 6a, 79  | `RIGID_BODY_SERIALIZER` / `COLLIDER_SERIALIZER` / `MOTION_COMPONENT_SERIALIZER` all ship                   | `packages/physics/src/serializers.ts`, `packages/motion/src/serializers.ts`            |
| `PH-18` | 114–117 | One closure with `A-22` (spec revision 1.7)                                                                | `docs/SPECIFICATION.md` (§97a)                                                         |
| `PH-19` | 20, 37  | `solver: "auto"` over a §37 capability-filtered registry                                                   | `packages/physics/src/solver-registry.ts`                                              |

### Tracking integrity — two batches that closed nine filings and told nobody

Verified by reading the commits and then the code, not the trackers:

- **`ab13840` (2026-08-06)** closed `PH-2`, `PH-3`, `PH-4`, `PH-7`, `PH-14`, `PH-15`, `PH-16`
  and `PH-1` stage 1. It touched **no** `CHANGELOG.md`, `MEMORY.md`, `TODO.md`, or gap-analysis
  banner. The only record outside the source is its commit message.
- **`fe8eb6f` (2026-08-06)** closed `R-11`, closed `R-12` at the base tier and `R-10` at sort
  key 2 — the campaign's own keystone — with the same silence.

All nine were re-verified in source for this document and are reported as closed above. But
v0's banner set, `CHANGELOG.md`'s Unreleased section, and `TODO.md` all still read as though
these findings were open, which is the exact failure mode v0's `A-28` was written about,
pointed at the gap analysis itself. Repairing it is Group 0 of §4.6's recommended order and
costs nothing but typing.

---

## 3. Partially closed

| ID     | Shipped                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Precisely what remains                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Tracked in                                                                                                                                                                                                                                                                                                                                                    |
| ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `A-4`  | §85's build-mode half: `DEV`/`devWarn`/`devWarnOnce`/`devAssert` behind `__FOUR_DEV__`, allowlist-enforced (`packages/core/src/dev.ts`)                                                                                                                                                                                                                                                                                                                                                                                                            | The §85 **validation catalogue** in `@four/diagnostics`; converting scattered scene/physics checks to `devAssert`; routing §42's authority-conflict warn through the channel; §41's 1e5 envelope check                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `TODO.md` "A-4 remainder"                                                                                                                                                                                                                                                                                                                                     |
| `A-5`  | §83 accounting tier: `byteLength` + live-instance counters for geometry/texture/render target; `textureMemory`/`bufferMemory` measured                                                                                                                                                                                                                                                                                                                                                                                                             | All six §83 development **warnings**; creation-site capture; `AssetManager` duplicate-load warning; materials and solver handles unaccounted                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `TODO.md` "A-5 remainder"/"A-5 follow-ups"                                                                                                                                                                                                                                                                                                                    |
| `A-12` | Six controls (`Toggle`, `Checkbox`, `RadioButton`, `Slider`, `ProgressIndicator`, `ImageWidget`) — 9 of §73's 16                                                                                                                                                                                                                                                                                                                                                                                                                                   | Text input (§56/S-6), scroll view + virtual list (§74 + §67), list, canvas view, embedded viewport (§48), menu + tooltip (need a widget-reachable §9 clock)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `UI_STAGED[0]`, `TODO.md`                                                                                                                                                                                                                                                                                                                                     |
| `A-13` | Keyboard half: `collectFocusOrder`/`keyboardFocusTarget`/`installKeyboardTraversal`, Enter/Space activation                                                                                                                                                                                                                                                                                                                                                                                                                                        | DOM mirror, screen-reader updates, high contrast, scalable text (all behind a DOM integration policy); reduced motion (behind `A-6`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `UI_STAGED`, `TODO.md` "A-13 PARTIAL"                                                                                                                                                                                                                                                                                                                         |
| `A-16` | §79 pairs for `Renderable`, `Sprite`, both cameras, `DirectionalLight`; resources by key via `SceneResourceCatalog`                                                                                                                                                                                                                                                                                                                                                                                                                                | §79's asset **manifest** (key → URL + content hash, blocked on `A-18`); the §80 `.four` binary format                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `TODO.md` "A-16 remainder"                                                                                                                                                                                                                                                                                                                                    |
| `A-18` | Deadline + size limits (via `A-23`); **caller-driven cancellation and transport `AbortSignal`** — generic `FetchLike<TSignal>` + injected `abortController`, refcounted abort, `canAbortTransport` (2026-08-09); 7 of 13 §76 capabilities                                                                                                                                                                                                                                                                                                          | Streaming, dependency graphs, progress reporting, worker decoding, hot reload, content hashing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `packages/assets/src/asset-manager.ts`, `TODO.md` "A-18 remainder"                                                                                                                                                                                                                                                                                            |
| `A-21` | `first-3d-scene`, the §118 flagship, and §119's `flagship/motor-digital-twin` (2026-08-08), all browser-gated; nine runnable examples                                                                                                                                                                                                                                                                                                                                                                                                              | only the three §93 stand-in directories (`first-animated-scene`, `first-physics-scene`, `mixed-scene`) are placeholders awaiting an owner retire-or-write call                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `docs/AUDIT-120.md` S-8, `TODO.md`                                                                                                                                                                                                                                                                                                                            |
| `A-24` | 61, 92                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Medium                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | S                                                                                                                                                                                                                                                                                                                                                             | **CLOSED (renderer half) 2026-08-08** — the context-loss suite exists at three tiers (9 unit + 7 integration + 1 real-Chromium browser via `WEBGL_lose_context`; the `NullRenderer.events` seam finally in use). No product bug found: the loss path was correct before it was tested — a behavioural gap, not a coverage one. One §83 corner in the owner register: dispose-after-failed-restore leaks rebuilt programs; the fix breaks the tested no-GL-while-lost property. Still open under A-24: the other §92 categories (scene+renderer, picking, assets+materials — the last waits on A-19). |
| `A-25` | Changesets config, `apply-publish-names.mjs` (+tests), `release.yml`, `docs.yml`, minimal `website/`                                                                                                                                                                                                                                                                                                                                                                                                                                               | The real site (guides hosting, flagship page); three owner steps: stub packaging, `NPM_TOKEN`, Pages enablement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `TODO.md` "A-25 owner decisions"/"A-25 remainder"                                                                                                                                                                                                                                                                                                             |
| `R-10` | §66 sort keys 1, 2 and 5 in `buildRenderList` (opaque before transparent, default-opaque classification keeping scenes byte-identical), key 3 as `groupRenderListByPipeline` (2026-08-09) with `RenderItem.materialId` as the material half, and **key 4 as `sortRenderListByDepth` (2026-08-09, R-8)** — opaque near-to-far, transparent far-to-near, on a view's own list, with `RenderItem.viewDepth` as the measurement. All four sort keys §66 lists for this tier now exist                                                                  | Keys 3 and 4 are alternative verbs rather than one comparator: §66 orders them pipeline-then-depth, which gives depth ordering only _within_ a material group and so destroys the one thing depth sorting is for (blended surfaces compositing in order). A single comparator carrying both is a real design with a real question behind it — which key wins for transparent content, and whether the answer differs per group — and is staged rather than guessed. Neither key is a default, and the reason is unchanged: co-planar opaque draws are order-dependent under `LEQUAL`, so either sort repaints a 2D scene. Also: OIT, weighted-blended, depth-prepass control, alpha-to-coverage, premultiplied-alpha policy; per-triangle transparent ordering | `packages/render/src/{render-list,view-list}.ts`, `packages/render/tests/view-list.test.ts`, `tests/integration/{render-batching,view-culling}.test.ts`                                                                                                                                                                                                       |
| `R-9`  | §65 **sprite batching and compatible shape batching**: consecutive items sharing a pipeline and a material instance merge into one `drawElements`, opt-in per renderer (`createGlBatching`). Backend-independent planner + WebGL 2 consumer, drawing through the existing unlit program — no new pipeline, no shader edit. Proven pixel-identical on ANGLE/SwiftShader (0 of 76 800 pixels differ; 13 draws → 3)                                                                                                                                   | Glyph batching (needs `R-28`), instanced meshes (`R-22`), atlas grouping of distinct textures, persistent-mapped/staged buffers and multi-draw (not core WebGL 2), batching the shaded pipelines (needs per-item normal matrices, i.e. instancing), batching on by default (needs A-4's build-time pipeline-selection seam: the seam costs every bundle +0.17 kB as it is)                                                                                                                                                                                                                                                                                                                                                                                     | **Moved to the partially-closed table 2026-08-09** — the consecutive-run tier shipped (sprite + compatible-shape batching, opt-in, pixel-identical on SwiftShader, two §86 rows to half-measured); see §3 for the shipped/staged split. The residue there (instancing, glyph batching, atlas grouping, default-on via A-4's seam) is what remains of §65/§86. |
| `R-12` | The abstract `Material` base with §57's six shared members; all three concretes re-parented; `Renderable<M>` generic                                                                                                                                                                                                                                                                                                                                                                                                                               | Eight §57 family members (`ShapeMaterial`, `TextMaterial`, `LineMaterial`, `StandardMaterial`, `PhysicalMaterial`, `ShaderMaterial`, `NodeMaterial`, `ComputeMaterial`); `stencil` (needs `R-7`); `RenderItemKind` is still a closed union, so a consumer still cannot register a pipeline _Unchanged by `R-16` (2026-08-09), deliberately: §58 landed and `ShapeMaterial` was **still** not shipped, because the paints live on the node (where §50's own example puts them) and bake into per-vertex colour, so the class would carry nothing a backend reads. Eight missing §57 members remains eight; the row moves when the paint pipeline does._                                                                                                         | `packages/materials/src/material.ts`, RFC 0001 for the pipeline registry                                                                                                                                                                                                                                                                                      |
| `R-27` | `Sprite extends Renderable<SpriteMaterial>`; dispatch on `material.kind`, not `instanceof`; `Shape2D` + **twelve** §50 nodes covering all fourteen §50 rows (`R-23` 2026-08-09, `R-16` 2026-08-09); **`frustumCulled` landed 2026-08-09 with `R-8`**; **`Text` landed 2026-08-13 with `R-28`**, in the umbrella package rather than in `@four/render`, because the frozen §3.1 matrix forbids `render` from importing `@four/text` — it extends `Renderable` unchanged, so §49's hierarchy holds by inheritance and no consumer has a special case | `depthMode`; `material: Material[]` — still the one §49 field with no tier, and with a concrete counter-example beside it: `R-16` puts a shape's fill and stroke in **one** geometry with per-vertex colour rather than two materials, so the field is wanted for §54 submeshes, not for 2D; the family's remaining members (`Mesh`, `Line3D`, `PointCloud`, `CustomRenderable`)                                                                                                                                                                                                                                                                                                                                                                               | `packages/render/src/renderable.ts`, `packages/render/src/shape.ts`, `packages/four/src/text-node.ts`                                                                                                                                                                                                                                                         |
| `R-39` | `benchmarks/README.md` now separates "needs hardware" from "needs an engine feature", and carries the benchmark scripts                                                                                                                                                                                                                                                                                                                                                                                                                            | One of the three remaining unreachable rows fell with `R-28`: **animated glyphs is now a `half` row in both halves' sense** — `benchmarks/text-layout.mjs` measures the layout (~2.1 ms per 20 000 glyphs, ~107 ns each) _and_ the `Text` geometry rebuild (~14 ms, ~700 ns each), and reports the number that closed the blocker: 20 000 glyphs are **one** `drawElements` under §65 batching, 800 unbatched, against **20 000** texture-binds-and-draws before R-28. Submission stays GPU-blocked, exactly as the two batching rows say of theirs. Still unreachable: mesh instances (`R-22`); GPU particles need `R-1`. CI integration and trend reporting remain absent                                                                                    | `benchmarks/README.md`, `benchmarks/render-batching.mjs`, `benchmarks/text-layout.mjs`, `benchmarks/results/text-layout.json`, `TODO.md`                                                                                                                                                                                                                      |
| `PH-9` | §18 state-machine tier: `AnimationController`, typed transitions, parameters, latched triggers, its own determinism golden                                                                                                                                                                                                                                                                                                                                                                                                                         | Blend trees; layered and additive animation; clip events from a controller; "any state" transitions                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | dated notes in `packages/animation/src/controller.ts`, `TODO.md`                                                                                                                                                                                                                                                                                              |

---

## 4. Open

Re-analysed from today's vantage. Severity and effort are restated against what now exists;
where a v0 blocker fell during the campaign it is named, because several items are materially
cheaper than v0 estimated.

### 4.1 Rendering — the tier that is now the project

| ID     | §       | Sev         | Eff | Was blocked by                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | State today                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------ | ------- | ----------- | --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `R-15` | 60a     | major       | M   | nothing (always unblocked)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **CLOSED 2026-08-08** at the policy + opt-in-transform tier: `@four/math`'s colour tier (`ColorRGB` hoisted, sRGB↔linear odd-extended, CSS subset parser, `color.ts` 0% → 100%), §60a resource metadata on textures (`SRGB8_ALPHA8` allocation) and render targets (double-encode refusal at validate), and the sRGB output transform as §63's final pass (fourth `ScreenEffect` member). Two dated opt-in-instead-of-default deviations so no golden moved; tone mapping staged on R-4's float formats; CSS-string options refused per §101's mapping row (owner question). Unblocks R-17's exposure work and R-6 tier 2.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `R-17` | 68      | major       | M   | **CLOSED 2026-08-09** at the eight-lamp forward tier: `PointLight`/`SpotLight` over a shared `PunctualLight` base, `MAX_PUNCTUAL_LIGHTS = 8` uniform-array set collected in the one §6 walk that finds the sun, `KHR_lights_punctual` falloff + cones, both shaded pipelines looping through one shared GLSL chunk, §79 pairs. Byte-identical for directional-only scenes (`FRAME_BEFORE_R17`, recorded; goldens unmoved). Deliberately: still exactly one directional light — a second sun is the clustered path's job. Hemisphere/area staged with arguments. +1.1 kB gzip; three budgets bumped with measurements.                                                                                                                                                                                            |
| `R-13` | 59      | major       | L   | `R-12`, `R-19`, `R-15`, `R-17`, `R-31`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **CLOSED 2026-08-08** at the scalar + base-colour-map tier: `StandardMaterial` (baseColor/map/metalness/roughness/emissive) + a sixth `StandardProgram` pipeline (Cook-Torrance GGX/Smith/Schlick against §68's one light, 1/π folded out of both lobes so the family composes with `LitMaterial`). The `R-17` dependency proved not to be a blocker (GGX vs Lambert under one light is real PBR); the `R-15` dependency stays open honestly — the BRDF computes in the same untagged space, so R-15 moves both families at once. `normalMap` waits on R-19's tangents; the other §59 maps on §77's unit allocator.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `R-18` | 69      | major       | L   | `R-4`, `R-5`, `R-17`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **CLOSED 2026-08-09** at the directional-shadow-map tier: `castShadow` + a validated `DirectionalLightShadow` (mapSize / bias / normalBias / extent / near / far), R-4's samplable-depth residue as `RenderTarget.depthTexture` (`DEPTH_COMPONENT24`), a seventh depth-only caster pipeline run as a renderer-internal pass before the view loop (§63 lists shadow passes as a _stage_, not an `addPass` example), and 3×3 PCF in both shaded pipelines through one shared GLSL chunk. §49's `castShadow`/`receiveShadow` land with it; §79 pairs additive. Byte-identical for non-casting scenes (`FRAME_BEFORE_R18`, recorded on the reverted build and byte-identical to R-17's); goldens unmoved; 59/59 browser gate. Found and fixed an F13 leak the pass exposed (on-screen frames can now bind a framebuffer). Deliberately: one map, one caster light — point/spot shadows need a cube map and a per-light index, i.e. §69's atlas; cascades, transparent masks and contact shadows staged with arguments. +2.4/+2.0/+1.8 kB gzip; three budgets bumped with measurements. `R-27`'s residue shrinks to `depthMode` / `frustumCulled` / `material[]`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `R-9`  | 65, 86  | major       | L   | `R-10`, `R-19`, `R-30`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `R-19` fell. Now gated on `R-10` keys 3–4 (without a pipeline sort no batch ever becomes contiguous). Two §86 rows are unreachable until it lands.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `R-8`  | 64, 87  | major       | M–L | `R-38`, per-view list                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **CLOSED 2026-08-09** at the per-view-list-plus-frustum-cull tier. The frame builds one render list; each viewport derives its own with `buildViewRenderList` (§46 mask by §48's fallback rule, then §87's frustum), keeping order — a subsequence, so every upstream §66 key survives and a view that filters nothing is the frame list, item for item. `@four/math`'s `Frustum` (six normalized planes, both `DepthRange` conventions, conservative `intersectsSphere`) and `@four/render`'s `computeWorldBoundingSphere` (§53's cached local box → world sphere by the absolute-value transform, never too small) are the substrate §87 lacked; §49's `frustumCulled` lands with them, defaults `true`, and round-trips through §79. **Design decision, measured**: derive rather than traverse per view — traversal has side effects (a particle system repacks, a sprite rebuilds its quad), §69's map is frame state no view may filter (R-18's §46 argument, now pinned by two tests), and `benchmarks/view-culling.mjs` puts the rejected alternative at 1.34–1.39× at two views and 1.90–2.65× at four. Byte-identical for a scene with nothing off screen (`FRAME_BEFORE_R8`, recorded on the reverted build); **0 of 76 800 pixels differ** on ANGLE/SwiftShader with 19 draws → 10 (`tests/browser/culling.spec.ts`); goldens unmoved; 61/61 browser gate. §33: plane extraction and the world radius are `same-runtime` (`sqrt`) and reach no solver, checksum or snapshot. §61/§85: nothing throws in a frame — every degenerate input fails **towards drawing**. Deliberately: no spatial index and no occlusion (a linear scan with an O(1) test is what §87's "the public scene graph must not be forced to mirror a spatial tree" allows a first tier to be); a §36 particle system is never culled, because its item carries the shared instance quad rather than a bound over its live particles. Unblocked and closed `R-10`'s key 4 in the same batch. +0.77 kB gzip in every bundle; three budgets bumped with measurements. **Found and fixed:** three integration harnesses were asserting draw counts for frames that produced no pixels (`{ height, aspect }` is not `OrthographicCameraOptions`, and a camera at the origin cannot see `z = 0`). |
| `R-38` | 46      | minor       | S   | nothing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **CLOSED 2026-08-08.** §46's registry, `Node.layers`, §47's `Camera.layers`, §48's `Viewport.layerMask`, §64 stage-2 filtering in both builders, `RenderItem.layers` for per-view masking from one list, §85 validation. Self-only inheritance (decision: identity, not state; `applyLayers` is the subtree spelling). Byte-identical in GL, goldens unchanged, +120 B gzip. Unblocks `R-8` (mask parameter already tested), `R-37` (`ScreenCamera`'s missing half), §71 picking filters (`layersMatch` is the whole predicate), and the flagships' viewport follow-up (proved: 7 drawables × 2 disjoint views = 7 draws).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `R-7`  | 67      | major       | M   | `R-12`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `R-12` fell; `stencil?: StencilState` is the declared hole in the new base. The GL context is still acquired with `stencil: false`, so this needs a context re-creation. Blocks §73's scroll view and §119's section views.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `R-25` | 52      | major       | L   | **CLOSED 2026-08-09 (polygon tier)** — `tessellation.ts`: `triangulatePolygon` (ear clipping + bridged holes; the deciding argument over monotone is determinism — sweep-line ties are where the claim quietly dies), the `PolygonTessellator` seam, `polygonGeometry2D`; the repo's first **cross-platform-tier** §33 golden (exactly-rounded ops only); simplicity proved before clipping (a pentagram succeeds _wrongly_ otherwise); 26 641 hole-free/single-hole fuzz cases with zero failures, multi-hole ~2/1000 refused-never-wrong (in-source table). `extrudeGeometry`'s concave-cap refusal lifted. Stroke/fringe → R-16; adaptive subdivision + incremental rebuild → R-24; self-intersections refused pending a fill rule. **R-24's fill half is unblocked; R-23 can fill all 14 §50 shapes today.** |
| `R-24` | 51      | blocker     | L   | **CLOSED 2026-08-09 (model + flatten tier)** — `Path` in `@four/geometry` (§98's placement): six segment kinds, fluent builder as the well-formedness invariant, 13 of 17 §51 operations (offset → R-16, the four booleans → the shared planar-subdivision packet). First two-tier §33 golden (Béziers cross-platform with a mechanical dyadic-rational proof; arcs same-runtime, values _and_ sample count). `fillRings` answers §51's fill rules and §52's outline/holes shape at once — islands are their own region. Every fillable §50 shape proven against analytic areas; three real bugs found by oracles, not review. Zero bundle bytes (A/B byte-identical).                                                                                                                                           |
| `R-23` | 50      | blocker     | L   | `R-12`, `R-16`, `R-25`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **CLOSED 2026-08-09 (solid-fill tier)** — `Shape2D` + nine nodes in `@four/render` (`Circle`, `Ellipse`, `Rectangle` incl. §49's rounded case, `RegularPolygon`, `Polygon`, `Star`, `Sector`, `Ring`, `PathShape`), covering **11 of §50's 14 primitives**; geometry entirely from R-24/R-25 (`toPath()` → `fillRings` → `triangulatePolygon`), validated F14 accessors with refuse-don't-clamp cross-field checks, owned lazily-rebuilt fill on a stable geometry id, §79 pairs for all nine. **Line, polyline and the open arc landed with `R-16` (2026-08-09)**, as this row predicted: they are stroke-only, §52 owns the expansion, and a joinless stroke is wrong rather than plain. `fill:`/`stroke:` as §58 paint literals landed with them; `ShapeMaterial` remains deliberately unshipped, re-examined rather than deferred (see `R-16`). Adds **no** render-item kind, **no** backend pipeline, **no** frame-path edit — proven by an identical GL transcript against equivalent plain `Renderable`s. Five of six bundles hash-identical; `motor-digital-twin` +10.46 kB (933.34/1000). Found: `Node.rotation` forbids `rotation` as a shape parameter (the family uses `startAngle`). Clipping/masks, booleans, world bounds, analytic hit testing staged with named owners.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `R-16` | 58      | blocker     | L   | `R-24`/`R-25`, `R-12`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **CLOSED 2026-08-09 (solid-paint + full-stroke tier)** — §58's `Paint`/`SolidPaint` (with §50's separate fill/stroke opacity), `ShapeFill`, and `StrokeStyle` **whole**: alignment, butt/round/square caps, miter/round/bevel joins with a limit that falls back to a bevel, and dashes with a phase offset — over `expandStroke` in `@four/geometry`'s tessellation module, which is where §52 puts stroke expansion by name. §50's `fill:`/`stroke:` options ship, and with **`Line`, `Polyline` and the open `Arc`** the family covers **all fourteen** §50 rows (twelve classes). The two colours travel as §53 per-vertex colour through `R-19`'s `useVertexColors` switch, so a stroked, painted shape adds **no `RenderItemKind`, no pipeline, no frame-path edit** — byte-identical GL transcript and geometry for any scene naming no paint, recorded on the reverted build; five of six bundles hash-identical, `motor-digital-twin` +3.62 kB. §79 pairs additive both ways (a pre-`R-16` document restores a fill-only shape). Second §52 golden at the **same-runtime** tier beside the fill tessellator's cross-platform one — offsetting needs `Math.sqrt` and a round join needs `Math.acos`/`cos`/`sin`, and `R-24`'s rule that a tier belongs to the operation forbids merging them. **Deliberately not shipped:** §58's six non-solid paints (per-vertex colour is exact for a solid and a two-stop linear gradient and silently faceted for everything else; the exact tier is a pipeline at the measured ~1.9 kB-per-bundle law plus a `RenderItemKind` arm `RFC 0001`/`RFC 0003` both want) and §52's anti-alias fringe (needs a coverage attribute no §57 pipeline reads). `ShapeMaterial` stays unshipped, now as a re-examined decision rather than a deferral.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `R-26` | 50      | major       | M   | `R-24`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **CLOSED 2026-08-09 (path-data tier)** — `svg-path.ts` in `@four/geometry` (§98's placement — the `d` attribute is the path model's serialized form; `render-svg` is a backend, `@four/assets` owns the file): `parseSvgPathData` / `formatSvgPathData`, **complete `d`-grammar coverage** (all ten commands both cases, implicit repetition, the greedy `1.5.5` split, packed arc flags; `B`/`b` refused), F.6.5 arcs both directions. `fromCommands` proved unnecessary — export reads `Path.commands`, so R-24's decision stands. **Y is transcribed, not flipped**: a flip without the `viewBox` is half a transform done silently, and the correction is one exact `Path.transform` the document tier will own. New rule: **a format conformance rule is not an §85 clamp** (F.6.6's radius normalizations are honoured; malformed text is refused with an offset, and nothing before the error is kept — unlike an SVG viewer). §96: no regexes, O(n) on every input, one finite bound, totality fuzzed over 30 000 strings. Second two-tier §33 golden (cross-platform text half proved dyadic + a byte-for-byte text fixed point; same-runtime arc half). Zero bundle bytes (A/B byte-identical). **Staged with named owners:** `<svg>` documents (needs an XML-reader seam decision — `DOMParser` is browser-only) and the shape elements (`R-23`'s classes now exist to receive them); `fill-rule` was already `Path.fillRule`, and **paints landed with `R-16` (2026-08-09)**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `R-28` | 49, 56  | blocker     | M   | `R-30`, `R-9` (to be _good_)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **CLOSED 2026-08-13 (bitmap-label tier)** — `Text` in `packages/four/src/text-node.ts`: a real `Renderable<UnlitMaterial>` turning a string into **one** indexed vertex buffer of glyph quads with per-vertex uv over **one** atlas material. One draw call per label _unconditionally_, and consecutive labels over one material merge into one draw under §65 — so §65's glyph-batching strategy is closed at the label level with nothing added to `batch.ts`, and its residue is atlas grouping. **Adds nothing to the frame path** (no `RenderItemKind` arm, no pipeline, no shader edit; `render-webgl` opened only for `R-30`): third application of `R-23`'s rule, and the concrete reason the N-sprites design was rejected — §55's affine uv cannot map twelve glyphs onto twelve cells in one buffer, but §53's `uvs` (R-19) and §57's `map` can. Placement is forced, not preferred: the frozen §3.1 matrix forbids `render` from importing `text` and vice versa, and the umbrella is the one package that sees both (§49's family is already split this way — `ParticleSystem` lives in `@four/particles` and cannot even extend `Renderable`; `Text` sits _above_ `render` and genuinely does). Ships: bitmap text, Latin layout, `\n` breaks, baseline metrics, letter spacing, and §56 **horizontal alignment** (new `TextAlign` on `layoutText`). §79 pair `render:text` with the font through the `atlas` option `Label` already used, refused loudly when absent. `castShadow` defaults `false` here alone (a depth-only pass writes geometry, not alpha). §33 stated **cross-platform** for layout, mechanically. Deliberately staged: SDF/MSDF, wrapping (a word-breaking rule, i.e. UAX #14), vertical alignment, rich spans, text on paths, billboard/screen space, carets, the §75 mirror; §56's `fontFamily`/`fontWeight`/`color` are absent rather than accepted-and-ignored. Gates: a new real-GL browser spec, a new visual golden, and a cross-package suite proving a label emits the transcript of a plain textured `Renderable`.                                                                                                                                                                                                            |
| `R-29` | 55      | minor       | S   | nothing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **PARTLY CLOSED 2026-08-08 — the `frame` half.** `Sprite.frame` in texels (bottom-left origin), `SpriteRenderItem.frame`, §85 containment refusal, WebGL resolution into the generalized `quad` uniform — the 2026-08-07 retire-the-uniform prediction is retracted with the affine-reparametrization derivation that disproves it. Frameless sprites byte-identical by code path (tenth run). +480 B; ui-demo 32.98/33 kB (20 B left — next bundle-touching packet needs a proposal). Staged dated: named-frame atlas (→ `@four/assets`), sprite animation clips (→ §14/§17 step tracks). §55 now 5 of 11.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `R-30` | 77      | major       | M   | nothing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **PARTIALLY CLOSED 2026-08-13 (sampler-state tier)** — §77's _wrap and filter modes_ bullet ships: `TextureSource.filter` / `.wrap`, resolved on `Texture`, mirrored as optional `MaterialTexture` fields, applied by `TextureCache` at upload. Both default to the previously hard-coded `LINEAR` + `CLAMP_TO_EDGE`, so every existing texture issues the identical four `texParameteri` calls — byte-identity is structural. §85 refuses an unknown value rather than substituting. One `filter` rather than `minFilter`/`magFilter` (the other four GL values choose between mip levels, and this tier has one); one `wrap` for both axes (nothing authors anisotropic addressing yet); both widen without moving. **Finding: this row never blocked `R-28`.** GAP v0's dependency line reads "R-30 (frame regions)", and frame regions are §55's — they shipped as `R-29` on 2026-08-08. The v1 blocker cell inherited a stale name; second instance of `R-8`'s rule that a blocker sentence naming another row's residue must be re-checked against source. Still open: mipmaps and generation, cube/array/3D targets, anisotropy, §77 map roles, compressed containers, video textures, `ImageBitmap`/canvas sources, async upload + residency diagnostics. Measured +0.11 kB gzip in every bundle carrying `Texture`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `R-22` | 54      | major       | L   | see RFC 0003 for the skinning rows                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Instancing, LOD, morph targets, multi-material groups, dynamic buffers all absent; the instancing row makes one §86 row unreachable. Non-skinning rows are **still silent**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `R-21` | 53      | major       | S–M | nothing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | No `Geometry` base, no `clone()`; `bounds` is an AABB not a `BoundingVolume` — and `R-8` no longer needs one (2026-08-09): `@four/render`'s `computeWorldBoundingSphere` derives the world sphere culling wants from the AABB, and the volume hierarchy §53 spells is still `R-21`'s.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `R-3`  | 62      | major       | S+M | nothing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Still 2 of 11 capability categories, and no required/optional declaration API. The S half is `gl.getParameter` calls; `A-1`'s `gpuFrameTime` and `A-26`'s generated backend table both wait on it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `R-1`  | 62      | major       | L   | `R-14`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Four backend packages are still one-line stubs. `R-2`'s ladder is exercised against doubles because only one backend is registrable — the registry's upper rungs are unproven.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `R-31` | 36, 112 | minor       | L   | `R-1`, §82                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `R-32` | 36      | minor       | M   | `R-19`, `R-12`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Both blockers fell; textured/rotated/soft particles are now ordinary work.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `R-33` | 112     | minor       | S   | nothing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | §112's exit is still proven for simulation only; a 100k browser page against the shipped instanced path would close it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `R-34` | 27, 112 | minor       | M   | nothing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Unchanged: ~5.1 ms per field per 100k particles; batch `sampleAll` is the scoped fix.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `R-36` | 44, 47  | major       | M   | nothing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **CLOSED 2026-08-09 (helper tier 2026-08-09, rig tier same day).** Both halves have landed. Helper half: `Node.lookAt(target, up?)` and `Node.getWorldDirection(out)` over `Quaternion.setFromLookDirection` — −Z confirmed as every node's forward, world-space target with the parent's world rotation divided out, §85 refusals, pinned against an independently-built gluLookAt matrix. Rig half: `@four/motion` ships `OrbitRig` (§44 orbit), `FollowRig` (§44 follow target **and** spring arm — one class switched by `frame`, smoothed by `SpringDamper`) and `LookAtConstraint`, advanced by `ConstraintSystem` at §39 step 7 under §42's `"constraint"` authority. A rig **places**, a constraint **aims**, both in one system because §42 allows a node exactly one owner. §44's _path animation_ and _physics attachment_ close by composition rather than by a class (a path-driven parent under `"kinematic"` carrying a constraint-aimed child; a rig reads a target's transform and writes only its own node, and step 7 runs after step 6). **Deliberately staged, with reasons and owners: fly** (reduces to one line of application code once input is fed in), **first-person** (writes a rotation, so it needs the aim-vs-free-look arbitration §12's character controllers will decide), **trackball** (defined in screen space, which `motion` may not import under the frozen §3.1 matrix — belongs with `ScreenCamera`), **shake/impulse** (wants interpolated value noise, not per-step white noise, which would change character with the fixed rate), **stereo/XR** (an extension point, not a rig). §33 tier `same-runtime` with a new golden. Bundle cost measured A/B: **0 B** in five of six budgets, **+2.8 kB gzip** in the one bundle that calls `registerSceneNodeTypes()`.                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `R-37` | 47, 48  | minor–major | S–M | `R-38`, `R-4`, `R-5`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `R-4`/`R-5` fell, and **§48's `layerMask` was never `R-37`'s to ship**: `Viewport.layerMask` plus `viewLayerMask`'s §48 fallback landed with `R-38` on 2026-08-08, and `R-8` moved the filter from a draw-time test to build-time on 2026-08-09 (the `R-8` row's claim that "`R-37`'s `layerMask` waits on the per-view restructure" was stale when written). What remains is `ScreenCamera` — top-left / bottom-left / centered origins in logical or physical pixels. §47's camera rigs are no longer part of this row: orbit, follow target and spring arm shipped with `R-36`'s rig half on 2026-08-09, and of the staged remainder only **trackball** touches this row — it is defined over a viewport in screen space, which `@four/motion` may not import, so it belongs to the `ScreenCamera` packet rather than to the motion tier. This is why the flagship parents its UI panel to the camera.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `R-41` | 119     | —           | —   | **Closed as evidence 2026-08-08** — `examples/flagship/motor-digital-twin` ships and is browser-gated (9 measuring tests). Charts are the documented lines+vertex-colour workaround (`R-24` still open); the instrument panel is camera-parented (`R-37` still open); the torque/force glyphs are the twin's labelled estimate (no adapter reports joint reactions). None of the three blocks the demonstration.                                                                                                                                                                                                                                                                                                                                                                                                 |

### 4.2 Application

| ID     | §      | Sev    | Eff | State today                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------ | ------ | ------ | --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `A-6`  | 45     | High   | M   | **CLOSED 2026-08-08** (except two recorded refusals): `physics` (instance or poses-receiving factory; §45's literal options form deferred for the bundle reason — third deferred-string-selection instance), `assets`, `autoResize` (injected `SurfaceObserver`), `reducedMotion` (`"auto"`/boolean via injected source, resolved per read). `app.input`/`app.diagnostics` refused per the §40 precedent — §45 names them in prose only. Discharges A-1's `physicsStepTime` + `activeBodies` (only `gpuFrameTime` + `contacts` remain staged, each with its reason), A-13's reduced-motion policy half, and PH-22m's policy half (§14's animation-side opt-in hook still open). |
| `A-11` | 71     | Medium | L   | **Addendum 2026-08-09:** the `R-23` half fell — every §50 shape answers `toPath()` with a §51 `Path`, so analytic hit testing has its geometry. Still blocked on the pixel/GPU-id half: a render target `@four/input` may not import. Wants an RFC, not a packet.                                                                                                                                                                                                                                                                                                                                                                                                               |
| `A-19` | 77, 78 | Medium | L   | Unchanged. glTF now has three blockers rather than four (materials, textures, skinning) — RFC 0003 answers the third on paper.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `A-20` | 82     | Low    | L   | **Still literally silent.** No `ComputePass`, and still no dated line in `packages/render-webgpu/README.md` or `TODO.md`. The cheapest open item in the document: one sentence.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `A-27` | 86, 92 | Medium | M   | **Partly closed 2026-08-08.** Seven scripts, a runner (`run-all.mjs`, `pnpm bench`), `SUITE` exported from `harness.mjs` so index and tree cannot drift. The two CPU rows v0 named are measured — retained-UI layout (11.7 ms cold at 5 000 widgets) and glyph layout (2.19 ms at 20 000) — both **half-rows**, both honest about which half (draw halves blocked on §55/§65 and a GPU; the README gains a `half` category). Still open: the non-gating CI trend job — a regression is only visible by reading `git diff` over `results/`.                                                                                                                                      |

### 4.3 Simulation

| ID      | §       | Sev         | Eff | State today                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------- | ------- | ----------- | --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `PH-8`  | 26, 27  | major       | M   | **CLOSED 2026-08-09.** §26's six methods and §27's built-in field set had both shipped (Phase 5; WP-9.2 in `@four/particles`) and nothing joined them — the gap was the generator seam, and `PRIORITY_FORCES` now has its engine occupant. `@four/physics` ships `ForceField` (§27 transcribed) and `ForceFieldSystem` at §39 step 5, plus `PhysicsWorld.forEachActiveBody` — the §22-dynamic/§32-awake, registration-ordered (§33) iteration with each body's solver-read world-space centre of mass (§25). Units are a **required** argument (`"force"` N / `"acceleration"` m/s² × mass) because §27's own list mixes them and `@four/particles` documents every field as an acceleration. The cross-package contract is **structural**: `ParticleForceField` is member-for-member identical, so every built-in field composes with no adapter and **no new §3.1 edge**, checked in `tests/integration/physics-force-fields.test.ts` (the only file that may import both). `world.step` was not edited, so every existing golden is untouched by construction; new golden `golden/force-fields.json` (same-runtime, real Rapier 2D). §27's volume inclusion needed nothing — `volumeField` already composes onto any conforming field. **Deliberately absent, with reasons in source:** field torque (§27's `sample` names no angular channel) and automatic waking of sleeping bodies (§32 would stop meaning anything; seam is a per-entry `wakesSleepingBodies` flag).                                                                                                                                                                                                                              |
| `PH-11` | 12, 44  | major       | L   | **CLOSED 2026-08-09 at tier.** The seam this row named is built exactly as recorded: a `LookAtConstraint` component plus a `ConstraintSystem` at `PRIORITY_CONSTRAINTS` calling `setFromLookDirection` per step under `"constraint"` authority, with a slew limit — spelled `maxAngularSpeed` in radians per second, `MotionComponent`'s vocabulary rather than a new one, absent meaning unlimited. §12's **look-at constraints**, **orbit motion** and **camera rigs** are closed (`OrbitRig`, `FollowRig`); `steering.ts` was not touched and keeps its no-node-access property. **Still open from this row: character controllers** — which is also what first-person camera control now waits on, since both want one yaw source and §42 gives their node one authority. A degenerate aim mid-simulation is a counted skip (`skippedSteps`), never a throw inside a fixed step; §85's refusals apply at authoring. Effort remaining: M, for the controller alone.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `PH-12` | 8, 21   | major       | M   | **CLOSED at the physics tier 2026-08-09; the node-level half is blocked and named.** `@four/core` now carries §8's vocabulary (`SpaceMode`, `SPACE_MODES`, `DEFAULT_SPACE_MODE`, `isSimulationSpaceMode`), hoisted for the `DEFAULT_GRAVITY_Y` reason — §8's two halves belong to pillars that may not import each other. `RigidBody.space` declares the frame a body is solved in and `PhysicsWorld.addBody` enforces §8's sentence, with **two distinct refusals**: the four presentation frames because §8 forbids automatic participation, `"local-plane"` because §21's plane→XY mapping is unbuilt (accepting it would solve in world space while the author asked for a plane). Default `"world"`, omitted from the descriptor there, so nothing existing changes a byte; round-trips through §79 as a defaulted field, because dropping it would turn a refused body into an accepted one after a reload. `isSimulationSpaceMode` answers **§8's** question, not the world's — the two differ exactly at `"local-plane"`, pinned by test. **Still open, blocked:** the node-level `NodeSpace` component (a screen-space panel is in screen space with or without a body). Its blocker is mechanical, not design: a component class carries a `static typeName`, `serializeScene` throws on a component with no registered serializer, and the registration site is `registerSceneNodeTypes` in the umbrella package. Class + serializer + the render-side consumer that gives `screen`/`viewport`/`camera`/`billboard` a meaning must land in one packet. The §21 `"local-plane"` frame itself (plane descriptor + mapping in the publish pass) is the other open half, refused loudly meanwhile. |
| `PH-20` | 33      | minor       | M   | Unchanged: five of §33's six ship; rollback has the primitives (`ReplayPlayer.seek`) and no API.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `PH-21` | 39      | minor       | M   | **Partly discharged 2026-08-09.** `PRIORITY_CONSTRAINTS` is no longer empty: `ConstraintSystem` (`@four/motion`) occupies §39 step 7 as a separate registered system, which is the split this row asks for — proving in passing that a new step can be occupied without editing `PhysicsWorld.step` (PH-8's technique, reused). Unchanged: `PhysicsSystem` still occupies one priority and performs steps 6, 8 and 9 internally, so `PRIORITY_SENSOR_UPDATE` and `PRIORITY_EVENT_DISPATCH` sit empty. Effort remaining: S–M.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `PH-22` | various | minor–major | —   | **Swept 2026-08-08**: `PH-22a` CLOSED (7 shapes shipped + measured Rapier facts; `compound` is composition by decision); `PH-22e` CLOSED (`jointMotorEffortCap` declared + warned); `PH-22f` PARTIAL (`collisionEnabled` live — the only mutable-with-throw joint property; anchors re-staged on the which-pose decision); `PH-22n` HALF (§41 diagnostics ship; the §10 dropped-time warning is app-tier). `PH-22b/c/d` blocked, re-verified against Rapier 0.19.3's declarations. `PH-22g/h/i/k/l/m` are cross-tier (animation/motion/math/core/four packets). `PH-22j` remains the owner's §102 scope decision.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

### 4.4 Design drafted, implementation open — the RFC-gated trio

All three are `docs/rfcs/*` drafts dated 2026-08-07 with **owner decision pending**; no packet
may start before acceptance, and each RFC names its own blocking question (see §5).

| Filing           | RFC                                       | What the RFC decides                                                                                                                                                          | What it unblocks                                                         |
| ---------------- | ----------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| `R-14`           | `0001-shader-and-node-material-system.md` | A serializable shader graph as the unit of extension; **no user GLSL/WGSL at any tier**; node materials as a separate lazily-compiled pipeline; one new `ScreenEffect` member | `R-1` (second backend), `R-6` tier 2, `R-13`, `R-12`'s pipeline registry |
| `A-3`            | `0002-plugin-system.md`                   | `PluginContext` as a typed capability bag; explicit registration only; no sandbox but documents can never name a plugin. Alternative E (do nothing) is argued as defensible   | §90's plugin-API row, §79's shipped promise                              |
| `PH-10` / `R-22` | `0003-skinning-and-skeletal-animation.md` | Joints/weights at attribute locations 4/5; `Bone`/`Skeleton` as scene nodes; skinned draws as a separate pipeline; §54's `morphTargetWeights` moves to a component            | §78 glTF (`A-19`), §17's two track types, `R-22`'s skinning rows         |

### 4.5 New items the campaign itself surfaced

Not v0 findings; recorded here because a re-analysis that omitted them would be incomplete.
All are tracked in `TODO.md` or `MEMORY.md`.

- **The cannot-tree-shake class.** Anything reachable from a class method ships in every
  bundle: `A-1`'s stats references (~0.4 kB gzip) and `R-6`'s fifth pipeline (0.75 kB). The
  ui-demo budget moved 30 → 31 kB on a proven structural conflict. `A-4`'s define helped
  (0.46–0.52 kB back) but the real fix is an opt-in registry split, unwritten.
- **§97a is already partially stale**, three weeks old: it records `AnimationController` as
  unshipped (`PH-9` shipped it), and `renderer: "auto"`/`solver: "auto"` as deferred (both
  shipped the same day). A spec-revisit item exists; the amendment does not.
- **`registerRapierSolver()` carries both wasm images** — the flagship pays 1.54 MB gzip for
  `solver: "auto"`. Per-dimension registration is the recorded fix.
- **`docs/Architecture/` graph artifacts are stale** for every export added after 2026-08-06.
- **A flaky browser gate** (`tests/browser/blending.spec.ts`, the RECOVER case) fails under
  load and passes in isolation; pre-existing, confirmed at baseline.
- Smaller: `INVALID_RENDER_GRAPH` is not in §89's `FourErrorCode` union; two pre-existing
  Prettier warnings; `packages/math/src/color.ts` sits at 0% coverage (and is where `R-15`
  will land); `examples-build-coverage`'s regex does not match nested example paths.

### 4.6 Recommended order of attack

Ordered by unblocked surface per unit of effort, with the ordering constraints made explicit.
Items inside a group are independent.

**Group 0 — free, do it first (hours).** Repair the tracking hole: write `CHANGELOG.md`
entries and v0 banners for `ab13840` and `fe8eb6f`, so nine closed filings stop reading as
open. Add the `A-20` sentence (§82) — the last literally-silent cheap item. Regenerate
`docs/Architecture/` (`pnpm graph`). None of this needs a decision.

**Group 1 — the ordering constraint (M).** `R-15` (§60a colour management). It must precede
`R-13` and `R-17`, it is unblocked, and every frame drawn before it lands is shaded in the
wrong space. Do not schedule the lighting work first.

**Group 2 — the cheap unblockers (S–M, parallel).** `R-38` (named layers → `R-8`, `R-37`,
picking filters, the flagship's viewport follow-up — _`R-8` closed 2026-08-09 on exactly the
mask parameter `R-38` pre-tested; `R-37`'s `layerMask` half turned out to have shipped with
`R-38` itself_); `R-3`'s field half (S, unblocks
`A-1`'s `gpuFrameTime` and `A-26`'s backend table); `R-29`'s `frame` (S, unblocks `R-28`);
`A-24`'s context-loss suite (~40 lines against the seam built for it); `A-27`'s two CPU
benchmarks plus a runner; `PH-21`'s system split.

**Group 3 — 3D catches up (L, sequential).** `R-17` (multi-light) → `R-13` (PBR) → `R-18`
(shadows), each after `R-15`. `R-10` keys 3–4 → `R-9` (batching) in parallel; that pair plus
`R-28` closes three §86 rows and the per-glyph-draw cost. _The lighting chain completed
2026-08-09 (`R-13` on 08-08, `R-17` and `R-18` on 08-09). **The batching arm completed
2026-08-09**: `R-10` key 3 shipped as `groupRenderListByPipeline` (key 4 is now blocked on
`R-8` and only on `R-8`), and `R-9` shipped sprite and shape batching, moving two §86 rows
from feature-blocked to half-measured and unblocking `R-28`'s "to be good" clause. **Group 3
is closed.** What remains of §65 is instancing (`R-22`) and **atlas grouping**: glyph batching closed on 2026-08-13 with `R-28`, and it closed as a `Text`-node packet exactly as predicted — a label is one geometry over one atlas material, so a run of labels is a run the existing batcher already merges. **And `R-8` closed on 2026-08-09**:
the per-view list arrived, key 4 shipped with it as `sortRenderListByDepth`, and §64's stage 3
exists for the first time. `R-8`'s own residue is occlusion culling and a spatial index —
neither of which blocks anything._

**Group 4 — the composition root (M).** `A-6`. It is one packet that discharges `A-1`'s three
remaining counters, `A-13`'s reduced-motion requirement, and `PH-22m`, and it removes the
hand-wiring every example still does.

**Group 5 — the 2D vector stack (L, the largest remaining product gap).** `R-25`
(tessellation) → `R-24` (`Path`) → `R-23` (shape nodes) → `R-16` (paints/strokes), with
`R-26` (SVG) after `R-24`. This is half of what §1 means by "2D graphics", it is where three
of the four remaining silent sections live, and it is the last real blocker on §119
(`A-21`/`R-41`) and on `A-11`'s analytic picking. _`R-25`, `R-24`, `R-26` (path-data tier), `R-23` (solid-fill tier) and `R-16` (solid-paint + full-stroke tier) all completed 2026-08-09. **Group 5 is closed.** The 2D vector stack fills and strokes all fourteen §50 primitives, imports and exports SVG path data, and adds nothing to the frame path. What is left is not a chain: gradients and patterns want the paint pipeline `RFC 0001`/`RFC 0003` will shape, the anti-alias fringe wants a coverage attribute, clipping wants `R-7`'s stencil, and booleans want the one planar-subdivision packet §52's self-intersection rule also waits on. `A-11`'s analytic picking is unblocked on its geometry half._

**Group 6 — owner-gated (see §5).** RFC 0001 → `R-14` → `R-1`; RFC 0003 → `R-22`/`PH-10` →
`A-19`'s glTF; RFC 0002 → `A-3`. Decide 0001 first: it is the one whose absence is compounding
(`R-1`, `R-6` tier 2 and `R-12`'s pipeline registry all wait on it).

**Group 7 — long tail, schedule normally.** _`PH-11` + `R-36`'s rig half both closed 2026-08-09: §44/§47's orbit, follow-target and spring-arm rigs ship in `@four/motion`, and §42's `"constraint"` authority finally has a producing system at §39 step 7. What is left of that thread is §12's **character controllers** (which first-person camera control now waits on) and four staged rigs with recorded reasons — fly, trackball, shake/impulse and the stereo/XR point._ `PH-20`, the `PH-22` roll-ups, `A-18`'s content hashing (which `A-16`'s §79 manifest waits on), `A-19`'s texture tier, `R-21`, `R-31`–`R-34`, `A-11`. _`R-30`'s wrap/filter half shipped 2026-08-13 with the `Text` packet; its mipmap/format remainder is re-filed as `R-30b` in `TODO.md`._

---

## 5. Owner-decision register

Every question the tree is currently waiting on, consolidated. Recommendations are the
analysis's, not decisions.

| #   | Question                                                                                                                                                                                                                                                                                                                                                    | Where it is recorded                                     | Recommendation                                                                                                                                                                                                           |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | **Accept RFC 0001** (shader graph, no user source text at any tier)?                                                                                                                                                                                                                                                                                        | `docs/rfcs/0001-…md`, `TODO.md`                          | Accept. It is the compounding blocker (`R-1`, `R-6` tier 2, `R-12`'s registry), and the no-source-text stance is what keeps `RenderGraph.validate()` meaningful.                                                         |
| 2   | RFC 0001 Q1: should `ShaderMaterial` (§57) ever exist, or be recorded as permanently unshipped?                                                                                                                                                                                                                                                             | RFC 0001 open questions                                  | Record it permanently unshipped with a §57 amendment note; a source-string material re-opens §96 and §63 opacity for one row.                                                                                            |
| 3   | RFC 0001 Q3/Q5: per-material uniform ownership for the MVP; whether §60 needs an amendments row for the "no raw source" narrowing.                                                                                                                                                                                                                          | RFC 0001 open questions                                  | Per-material for the MVP; yes to the amendments row — a normative narrowing belongs in the spec, not only in an RFC.                                                                                                     |
| 4   | **Accept RFC 0002** (plugin system) — or accept its Alternative E and defer §81 explicitly?                                                                                                                                                                                                                                                                 | `docs/rfcs/0002-…md`, `TODO.md`                          | Defer explicitly (Alternative E) **and** keep the capability design as a recorded deferral. Five of eleven extension points already work as package APIs; §90's plugin row can say "n/a — §81 deferred, decision dated". |
| 5   | RFC 0002 Q1: `ApplicationOptions.plugins` — spec amendment, standalone host, or dated departure? It contradicts the same-day §40 "don't invent §45 options" precedent.                                                                                                                                                                                      | RFC 0002 open questions, `TODO.md`                       | If §81 proceeds: amend §45 (option (a)). §40's precedent turned on the option being absent from the spec's own list; §81 has nowhere else to live.                                                                       |
| 6   | RFC 0002 Q3–Q5: revocability default, `RENDER_GRAPH` capability in the MVP, plugin API version start (`0.1.0` vs `1.0.0`).                                                                                                                                                                                                                                  | RFC 0002 open questions                                  | Conservative default (non-revocable); include `RENDER_GRAPH` as a differently-shaped capability with that stated; start at `0.1.0` — honesty beats a range that parses.                                                  |
| 7   | **Accept RFC 0003** (skinning) and its named question: the **bone-axis convention**.                                                                                                                                                                                                                                                                        | `docs/rfcs/0003-…md`, `TODO.md`                          | Accept, with the RFC's recommendation: impose no convention on the data model, pin +Y for helpers only. Reversing it later invalidates authored rigs, so decide before the packet.                                       |
| 8   | RFC 0003 Q3: §54's `morphTargetWeights` placement is unimplementable under the frozen §3.1 matrix — amendments row against §54, or a dated note?                                                                                                                                                                                                            | RFC 0003 open questions                                  | Amendments row. A spec statement that cannot be implemented under a frozen constraint should be corrected in the spec, not annotated in source.                                                                          |
| 9   | RFC 0003 Q2/Q5/Q6: four vs eight influences; refusal when a skeleton exceeds the joint limit; whether `Bone` is a subclass.                                                                                                                                                                                                                                 | RFC 0003 open questions                                  | Four (with locations 6/7 named as the extension point); refuse at setup with `UNSUPPORTED_GPU_FEATURE`; make `Bone` a subclass — §79 needs the `typeName`.                                                               |
| 10  | **`A-25` stub packaging**: the five reserved stubs cannot be Changesets-`ignore`d while `four` depends on and re-exports them. Publish them, drop the umbrella subpaths, or make them optional peers?                                                                                                                                                       | `TODO.md` "A-25 owner decisions", `.changeset/README.md` | Publish the stubs. Dropping the subpaths breaks §98's package tree; optional peers push a resolution problem onto consumers. A stub that says "reserved; not implemented" in its README is honest.                       |
| 11  | `A-25`: add the `NPM_TOKEN` secret; enable Pages (Settings → Pages → source "GitHub Actions").                                                                                                                                                                                                                                                              | `TODO.md`                                                | Owner-only steps; the workflows are inert without them and will stay inert safely.                                                                                                                                       |
| 12  | **§93 stand-in retirement (`A-21`/S-8)**: `first-animated-scene`, `first-physics-scene`, and `mixed-scene` are placeholder directories with shipped stand-ins (`first-2d-scene`, `physics-playground`). Retire the directories and repoint the guides, or write the three scenes?                                                                           | `TODO.md`, `docs/AUDIT-120.md` S-8                       | Retire two, write one. `physics-playground` and `first-2d-scene` already claim their roles; `mixed-scene` is §97's own example and, now that §97a exists and compiles, is cheap and worth having.                        |
| 13  | **Spec amendments waiting to be written** (all letter-suffix, none touching the frozen §1–120 numbering): §97a/§18/§100's `AnimationController` and `"auto"`-selection staleness; §57's family list having no `LitMaterial`; §17's track-type promise; §61's `createTexture`/`createRenderTarget` deferred-by-decision divergence.                          | `TODO.md` spec-revisit items                             | Batch them into one spec pass after RFC 0001/0003 are decided, so the §57 and §60 rows land together rather than twice.                                                                                                  |
| 14  | `R-6`/`A-1` payload policy: the ui-demo budget moved 30 → 31 kB on a structural conflict, and the opt-in registry split that would give it back is unwritten. Is the budget the constraint, or the split the priority?                                                                                                                                      | `MEMORY.md` 2026-08-07 (R-6), `TODO.md`                  | Treat the split as scheduled work, not a budget bump: §86's real number (150 kB) is distant, but "every example pays for every feature" compounds silently.                                                              |
| 15  | **Accept RFC 0004** (raster painting stack, drafted 2026-08-09 at the owner's request)? Unlike 0001–0003 it closes no filed gap — the spec chose retained mode and never asked for a painting API. A "no" carries a consequence: §73's canvas view should then be **withdrawn by amendment**, since the RFC shows its recorded blocker is wrong either way. | `docs/rfcs/0004-raster-painting-stack.md`                | Accept at tier (b) — the seam tier; the do-nothing recipe is 15 lines but leaves the first sanctioned content unbatchable/unpickable/unserializable, and the canvas view deserves an honest fate either way.             |
| 16  | RFC 0004 Q2/Q3: the §77a section number (+ `ALLOWED_LETTERED`); whether `CanvasTexture` defaults `colorSpace` to `"srgb"` while `TextureSource` defaults `"linear"` (deliberate — R-15's linear default only protects existing goldens; a new class has none — but two texture types in one package would answer differently).                              | RFC 0004                                                 | §77a; accept the split defaults with the reason stated on both.                                                                                                                                                          |
| 17  | RFC 0004 Q4/Q5/Q6: ship `CanvasViewWidget` without the render-tier seam (it names no texture type); constant-size refusal vs allowing resize with R-29's `frame` hazard; no update hook / staleness warning (the menu/tooltip §9 argument).                                                                                                                 | RFC 0004                                                 | Yes; keep the refusal (resize gates on R-30); accept no-hook.                                                                                                                                                            |

---

## 6. Cross-cutting observations

Brief by intent — each of these is recorded at length in `MEMORY.md`'s 2026-08-07 decision
entries, and this section exists to point rather than to duplicate.

**Byte-identity became the campaign's working discipline, and it is why the closures are
trustworthy.** Nearly every packet proved that the thing it did not change did not change:
`R-4`'s 449-call GL sequence comparison, `R-19`'s uniform-switch default seeded at GL's
initial `0`, `PH-1`'s deep-equalled adapter call sequences, `PH-6`'s neutralized-capture
reproduction of a golden's previous digest, `R-5`'s transcript-identical graph execution,
`A-4`'s pixel goldens run against a production bundle. The pattern generalises: _make the new
feature's absence indistinguishable from before it existed, then the goldens are evidence
rather than an obstacle._ It is the reason a campaign this large moved no determinism golden
except one envelope field, with proof attached.

**The duck-typed-contract count is now five, and it is the repository's answer to §3.1.**
`ParticleDrawable`, `ReplayTarget`, `DebugGeometrySink`, `isRenderTargetTexture`, and the
renderer-statistics transcription all let two packages agree on a shape without an import.
`A-5` deliberately did _not_ add a sixth (`recordResourceMemory` takes two numbers). The count
being tracked at all is the useful part: the pattern is load-bearing enough that its growth is
a decision, and cheap enough to be abused.

**The cannot-tree-shake class is the campaign's one accumulating cost.** Anything reachable
from a class method ships in every bundle whether or not the app asks for it — `A-1`'s stats
plumbing, `R-6`'s fifth pipeline. Two packets paid it, the third (`A-4`) bought some back
with a build-mode define, and the structural fix (opt-in registry splits, as `R-2`/`PH-19`
did for backends and solvers) is recorded and unwritten. It is the only place where "closed"
items made a shipped number worse.

**Multi-agent gotchas cost real time and are all recorded.** Never `git stash` in a shared
worktree (nine minutes of another agent's in-flight files); `git rebase --autostash` with
agents holding uncommitted work clobbers untracked files; the Playwright gate is not
concurrency-safe (seven fixed-port preview servers, `reuseExistingServer: false`); Vitest
transpiles without type-checking, so a changed cross-file signature only fails at
`pnpm run docs`. Add one from this document: **a batch that lands code without touching a
tracking file is invisible within a day** — `ab13840` and `fe8eb6f` closed nine filings and
were discoverable only by reading `git log`.

**The v0 root cause has been half-fixed.** v0's central finding was that staging discipline
was indexed on §120's MVP checklist, so sections §120 never names produced no record at all.
`tools/check-docs.mjs` now gates the mechanically checkable half, and the campaign gave §63,
§70, §84, §96, §90 owners. But the §-indexed `STAGED.md` that both v0 analysts independently
proposed **was never written**, and the sections still silent today (§82 and §54's non-skinning
rows) are exactly the ones it would have surfaced. Three of the original five — §58, §8, and
§26/§27 for rigid bodies — were closed by hand on 2026-08-09, which is evidence for the
recommendation rather than against it: each was found by re-reading the specification section,
not by any gate. Recommending it a second time: one row per specification section, checked by
the gate that already exists.

---

_End of Gap Analysis v1._
